export const ParagraphTools = () => {
  return null;

  /*
  <div>
    <ReversoLink text={paragraph.terms.map((_) => _.text).join(' ')} />
    <DeeplLink text={paragraph.terms.map((_) => _.text).join(' ')} />
  </div>
  */
};
